name: template # Change this

on:

  workflow_dispatch:

jobs:
  commands:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Commands to run
      
        run: |
          echo 1 # Change this
